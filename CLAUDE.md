# PA Agent — Instructions for Claude

## Web UI work

Before adding or modifying any functionality under `pa_agent/webui/` (backend or frontend), **read `docs/dev_guide/webui.md` first**. It is a router doc: read its project overview/directory map, then follow its task table to the specific `docs/dev_guide/0N_*.md` sub-doc(s) relevant to your task — not all of them.

This applies every time, even in a brand-new session with no prior context on this request.
